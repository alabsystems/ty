# Liveness Pattern Specs

Standalone specs for issue #1518 acceptance criteria:

- `AlwaysEventually` covers `[]<>P`
- `EventuallyAlways` covers `<>[]P`
- `WeakFairness` covers `WF_x(A)`
- `StrongFairness` covers `SF_x(A)`
- `LeadsTo` covers `P ~> Q`
