\* Unreduced ground-truth twin of FlatSymScale_n3.cfg. MUST be run with
\* --no-auto-symmetry: ty's automatic symmetry detection is ON by default and
\* rediscovers the same group, which would make this twin reduce identically.
INIT Init
NEXT Next
CONSTANTS
Procs = {p1, p2, p3}
nobody = nobody
INVARIANT TypeOK
INVARIANT Mutex
INVARIANT OwnerAgrees
